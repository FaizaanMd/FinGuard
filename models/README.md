# Models

Serialized model artifacts (`*.joblib`), experiment logs, and evaluation
results live in this directory. Artifacts are excluded from version control;
see `src/model_evaluation.py` for the experiment tracking format.